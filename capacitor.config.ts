import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scroll-bug-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
